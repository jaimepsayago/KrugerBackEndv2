server:
  port: 4444

spring:
  cloud:
    config:
      server:
        git:
          #clone-on-start: true
          #default-label: main
          uri: https://github.com/jaimepsayago/microservices.serverconfig
          default-label: main
security:
  basic:
    enabled: true